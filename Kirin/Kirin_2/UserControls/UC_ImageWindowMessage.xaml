<UserControl x:Class="Kirin_2.UserControls.UC_ImageWindowMessage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kirin_2"
             xmlns:settings="clr-namespace:Kirin_2.Properties"
             mc:Ignorable="d" 
             FontFamily="{DynamicResource MaterialDesignFont}"
             d:DesignHeight="140" d:DesignWidth="800">
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid>
        <Rectangle Fill="{Binding Source={x:Static settings:Settings.Default}, Path=Modal_bg, Mode=TwoWay}" MouseDown="Rectangle_MouseDown"/>

        <Border Name="modelBorder" BorderThickness="1" CornerRadius="5,5,5,5" BorderBrush="LightGray" Margin="7" Height="140" Width="580" Background="#F5F5F5">
            <StackPanel>
                <Grid>
                    <TextBox FontSize="26" HorizontalContentAlignment="Center" VerticalAlignment="Center" Margin="0,20,0,10" IsReadOnly="True" BorderThickness="0"
                  FontWeight="Medium" Foreground="DimGray" Padding="5" TextWrapping="Wrap" Text="Draw using yellow to highhlight issues or black to hide sensitive info"></TextBox>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
